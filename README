Global setup:
 Download and install Git
  git config --global user.name "Paul Martin"
  git config --global user.email architecture@gmail.com
        
Next steps:
  mkdir Test-Repo
  cd Test-Repo
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:architecture/Test-Repo.git
  git push origin master
      
Existing Git Repo?
  cd existing_git_repo
  git remote add origin git@github.com:architecture/Test-Repo.git
  git push origin master
